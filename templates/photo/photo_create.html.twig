{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Create Photo' %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('camera-fill') ~ ' Galleries', 'gallery_index'),
  bootstrap_breadcrumb(gallery, 'gallery_view', {id: gallery.id}),
  bootstrap_breadcrumb('Create Photo'),
) }}
{% endblock %}
