{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete Gallery ' ~ gallery %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('images') ~ ' Galleries', 'gallery_index'),
    bootstrap_breadcrumb(gallery, 'gallery_view', {id: gallery.id}),
    bootstrap_breadcrumb('Delete'),
  ) }}
{% endblock %}
